*---------------------------------------------------------------------*
*    view related FORM routines
*---------------------------------------------------------------------*
*...processing: YZ_VIEW_RULE_TAS................................*
FORM GET_DATA_YZ_VIEW_RULE_TAS.
  PERFORM VIM_FILL_WHERETAB.
*.read data from database.............................................*
  REFRESH TOTAL.
  CLEAR   TOTAL.
  SELECT * FROM YZTABL_RULE_TAS WHERE
(VIM_WHERETAB) .
    CLEAR YZ_VIEW_RULE_TAS .
YZ_VIEW_RULE_TAS-MANDT =
YZTABL_RULE_TAS-MANDT .
YZ_VIEW_RULE_TAS-TASK =
YZTABL_RULE_TAS-TASK .
YZ_VIEW_RULE_TAS-TASK_DESC =
YZTABL_RULE_TAS-TASK_DESC .
<VIM_TOTAL_STRUC> = YZ_VIEW_RULE_TAS.
    APPEND TOTAL.
  ENDSELECT.
  SORT TOTAL BY <VIM_XTOTAL_KEY>.
  <STATUS>-ALR_SORTED = 'R'.
*.check dynamic selectoptions (not in DDIC)...........................*
  IF X_HEADER-SELECTION NE SPACE.
    PERFORM CHECK_DYNAMIC_SELECT_OPTIONS.
  ELSEIF X_HEADER-DELMDTFLAG NE SPACE.
    PERFORM BUILD_MAINKEY_TAB.
  ENDIF.
  REFRESH EXTRACT.
ENDFORM.
*---------------------------------------------------------------------*
FORM DB_UPD_YZ_VIEW_RULE_TAS .
*.process data base updates/inserts/deletes.........................*
LOOP AT TOTAL.
  CHECK <ACTION> NE ORIGINAL.
MOVE <VIM_TOTAL_STRUC> TO YZ_VIEW_RULE_TAS.
  IF <ACTION> = UPDATE_GELOESCHT.
    <ACTION> = GELOESCHT.
  ENDIF.
  CASE <ACTION>.
   WHEN NEUER_GELOESCHT.
IF STATUS_YZ_VIEW_RULE_TAS-ST_DELETE EQ GELOESCHT.
     READ TABLE EXTRACT WITH KEY <VIM_XTOTAL_KEY>.
     IF SY-SUBRC EQ 0.
       DELETE EXTRACT INDEX SY-TABIX.
     ENDIF.
    ENDIF.
    DELETE TOTAL.
    IF X_HEADER-DELMDTFLAG NE SPACE.
      PERFORM DELETE_FROM_MAINKEY_TAB.
    ENDIF.
   WHEN GELOESCHT.
  SELECT SINGLE FOR UPDATE * FROM YZTABL_RULE_TAS WHERE
  TASK = YZ_VIEW_RULE_TAS-TASK .
    IF SY-SUBRC = 0.
    DELETE YZTABL_RULE_TAS .
    ENDIF.
    IF STATUS-DELETE EQ GELOESCHT.
      READ TABLE EXTRACT WITH KEY <VIM_XTOTAL_KEY> BINARY SEARCH.
      DELETE EXTRACT INDEX SY-TABIX.
    ENDIF.
    DELETE TOTAL.
    IF X_HEADER-DELMDTFLAG NE SPACE.
      PERFORM DELETE_FROM_MAINKEY_TAB.
    ENDIF.
   WHEN OTHERS.
  SELECT SINGLE FOR UPDATE * FROM YZTABL_RULE_TAS WHERE
  TASK = YZ_VIEW_RULE_TAS-TASK .
    IF SY-SUBRC <> 0.   "insert preprocessing: init WA
      CLEAR YZTABL_RULE_TAS.
    ENDIF.
YZTABL_RULE_TAS-MANDT =
YZ_VIEW_RULE_TAS-MANDT .
YZTABL_RULE_TAS-TASK =
YZ_VIEW_RULE_TAS-TASK .
YZTABL_RULE_TAS-TASK_DESC =
YZ_VIEW_RULE_TAS-TASK_DESC .
    IF SY-SUBRC = 0.
    UPDATE YZTABL_RULE_TAS ##WARN_OK.
    ELSE.
    INSERT YZTABL_RULE_TAS .
    ENDIF.
    READ TABLE EXTRACT WITH KEY <VIM_XTOTAL_KEY>.
    IF SY-SUBRC EQ 0.
      <XACT> = ORIGINAL.
      MODIFY EXTRACT INDEX SY-TABIX.
    ENDIF.
    <ACTION> = ORIGINAL.
    MODIFY TOTAL.
  ENDCASE.
ENDLOOP.
CLEAR: STATUS_YZ_VIEW_RULE_TAS-UPD_FLAG,
STATUS_YZ_VIEW_RULE_TAS-UPD_CHECKD.
MESSAGE S018(SV).
ENDFORM.
*---------------------------------------------------------------------*
FORM READ_SINGLE_YZ_VIEW_RULE_TAS.
  SELECT SINGLE * FROM YZTABL_RULE_TAS WHERE
TASK = YZ_VIEW_RULE_TAS-TASK .
YZ_VIEW_RULE_TAS-MANDT =
YZTABL_RULE_TAS-MANDT .
YZ_VIEW_RULE_TAS-TASK =
YZTABL_RULE_TAS-TASK .
YZ_VIEW_RULE_TAS-TASK_DESC =
YZTABL_RULE_TAS-TASK_DESC .
ENDFORM.
*---------------------------------------------------------------------*
FORM CORR_MAINT_YZ_VIEW_RULE_TAS USING VALUE(CM_ACTION) RC.
  DATA: RETCODE LIKE SY-SUBRC, COUNT TYPE I, TRSP_KEYLEN TYPE SYFLENG.
  FIELD-SYMBOLS: <TAB_KEY_X> TYPE X.
  CLEAR RC.
MOVE YZ_VIEW_RULE_TAS-TASK TO
YZTABL_RULE_TAS-TASK .
MOVE YZ_VIEW_RULE_TAS-MANDT TO
YZTABL_RULE_TAS-MANDT .
  CORR_KEYTAB             =  E071K.
  CORR_KEYTAB-OBJNAME     = 'YZTABL_RULE_TAS'.
  IF NOT <vim_corr_keyx> IS ASSIGNED.
    ASSIGN CORR_KEYTAB-TABKEY TO <vim_corr_keyx> CASTING.
  ENDIF.
  ASSIGN YZTABL_RULE_TAS TO <TAB_KEY_X> CASTING.
  PERFORM VIM_GET_TRSPKEYLEN
    USING 'YZTABL_RULE_TAS'
    CHANGING TRSP_KEYLEN.
  <VIM_CORR_KEYX>(TRSP_KEYLEN) = <TAB_KEY_X>(TRSP_KEYLEN).
  PERFORM UPDATE_CORR_KEYTAB USING CM_ACTION RETCODE.
  ADD: RETCODE TO RC, 1 TO COUNT.
  IF RC LT COUNT AND CM_ACTION NE PRUEFEN.
    CLEAR RC.
  ENDIF.

ENDFORM.
*---------------------------------------------------------------------*
